# Configuration for baseline logistic regression model

defaults:
  - default

model:
  type: "baseline"
  use_embeddings: true
  max_iter: 400

training:
  # Baseline doesn't use these, but keep for compatibility
  learning_rate: null
  batch_size: null
  num_epochs: null

output:
  output_dir: "outputs/baseline"

